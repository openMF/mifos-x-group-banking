# fork.properties — LOCAL, gitignored. Materialized from the org SoT so the deploy
# resolves the fork's identity/contact (config.rb#_fork_prop reads this file).
# SoT: workspaces/mifos-x/_org/company.yaml#deploy_contact + .../app-profile/app.yaml#org
#
# Populated to unblock the TestFlight EXTERNAL beta-review submission, which requires a
# non-empty contactEmail + contactPhone (Apple rejects empty ones). See config.rb
# beta_app_review_info { contact_email/contact_phone → _fork_prop("org.email"/"org.phone") }.

org.name=Mifos
org.email=team@mifos.org
org.first.name=Mifos
org.last.name=Team
org.phone=+919015090523
org.marketing.url=https://mifossave.pages.dev
org.privacy.url=https://mifossave.pages.dev/privacy
org.support.url=https://mifossave.pages.dev
