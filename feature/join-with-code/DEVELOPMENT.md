<!-- generated-by: kmp-viewmodel-gen -->
<!-- co-authored-by: kmp-screen-gen -->
# feature/join-with-code — Development

## 1. Module Identity

`:feature:join-with-code` — invite-code entry + group-preview confirmation flow (deep-link or
manual 6-char code → validate → preview → confirm join → personal-dashboard). Namespace
`org.mifos.groupbanking.feature.joinwithcode`. Source of truth:
`idea-layer/screens/join-with-code/{ui,api,docs,flow,data-flow,tests}.yaml`.

<!-- kmp-viewmodel-gen:BEGIN -->
## 2. Public API

**ViewModel constructor deps** (`JoinWithCodeViewModel`):
- `invitationRepository: InvitationRepository` (`core/data`) — `validateCode` / `fetchGroupPreview` / `joinGroup`.
- `authRepository: AuthRepository` (`core/data`) — `currentSession` (auth-gate for `OnConfirmJoin`).
- `analytics: KptAnalyticsTracker` (`core/analytics`).
- `crashReporter: CrashReporter` (`core-base/observability`).
- `inviteCode: String? = null` — deep-link nav-arg (`ui.yaml#nav_params.inviteCode`).

**State fields** (`JoinWithCodeState`): `inviteCode`, `inviteStatus`, `groupPreview`, `isJoining`,
`error` (`@Transient`).

**Action variants** (`JoinWithCodeAction`, 5 — verbatim mirror of
`ui.yaml#state_model.actions.members`): `OnCodeChange`, `OnValidateCode`, `OnConfirmJoin`,
`OnBack`, `OnRetry` — plus the sanctioned `Internal` async-result sub-interface
(`SessionChecked`, `ValidateFailed`, `ValidateExpired`, `ValidateAlreadyMember`, `PreviewLoaded`,
`JoinSucceeded`, `JoinFailed`).

**Event variants** (`JoinWithCodeEvent`): `NavigateToPersonalDashboard`,
`NavigateToLoginSignup(pendingInviteCode)`, `NavigateBack`, `ShowSnackbar(message)`.

## 3. Consumers

- `JoinWithCodeScreen.kt` / `JoinWithCodeRoute.kt` — generated by `kmp-screen-gen` in
  `feature/join-with-code/src/commonMain/kotlin/org/mifos/groupbanking/feature/joinwithcode/`
  (co-located with the ViewModel, same convention as `LoginSignupRoute.kt` /
  `GroupTypePickerRoute.kt` — NOT in `cmp-navigation`). `cmp-navigation`'s
  `build.gradle.kts`/`KoinModules.kt` already declare the `feature.joinWithCode` project
  dependency + `JoinWithCodeModule` include; wiring `joinWithCodeScreen(...)` into the app-wide
  `NavGraphBuilder` host is a follow-up (the same gap exists for `login-signup` —
  neither screen is yet registered on `RootNavNavigation`/`AuthenticatedNavbarNavigation`).
- `feature/join-with-code/src/commonTest/.../JoinWithCodeViewModelTest.kt` (12 tests, dispatches
  every Action; mirrors `tests.yaml` scenarios TC-JWC-001/002/003/004/005/006/008).

## 4. Boundaries

Reads/writes exclusively via the injected `InvitationRepository` + `AuthRepository`
(`core/data`) — no direct `InvitationApi`/HttpClient access from this module. No Store5
(`business_logic.kind: processor` — a validate-then-mutate flow per RULE-IMPLEMENT-STORE5-001,
so `NetworkResult` is consumed directly, not `.asScreenStream()`).

## 5. Data

See API.md#state / #actions / #events for the full field/payload schema.

## 6. Errors

`JoinError` (5 variants, verbatim mirror of `ui.yaml#state_model.errors.types`): `InvalidCode`,
`ExpiredCode`, `AlreadyMember`, `Network`, `Auth`. `NetworkError -> JoinError` mapping is
context-dependent (`OnValidateCode` vs. `OnConfirmJoin` — see `JoinErrorContext` KDoc in the
ViewModel file); every failure path sets `JoinWithCodeState.error` and emits a
`crashReporter.recordMessage(..., CrashSeverity.Warning)` breadcrumb. `JoinError.Auth` also
emits `NavigateToLoginSignup(pendingInviteCode)`.

## 7. Testing

`feature/join-with-code/src/commonTest/kotlin/org/mifos/groupbanking/feature/joinwithcode/JoinWithCodeViewModelTest.kt`
— 12 `@Test` functions covering initial state, deep-link auto-validate, code-change sanitize +
auto-validate threshold, successful validate → Preview, expired/already-member/invalid-code/
network error paths, retry re-dispatch, successful confirm-join → NavigateToPersonalDashboard,
unauthenticated confirm-join → NavigateToLoginSignup, confirm-join 400 → AlreadyMember, and
OnBack.

## 8. Observability

Kermit `Logger.i/w/e(TAG) { ... }` (`TAG = "JoinWithCodeViewModel"`) — PII-safe (logs group ids /
network-error names / status transitions only, never raw invite codes' PII-adjacent metadata).
`KptAnalyticsTracker.trackGroupOperation(operation = "join", ...)` fires on confirm-join success/
failure/unauthenticated-redirect. `crashReporter.setUser(session?.userId)` on every session-state
emission; `crashReporter.recordMessage` breadcrumb on mount + on every validate/join failure.

## 9. Evolution

- 2026-07-22 — `kmp-viewmodel-gen`: initial `JoinWithCodeViewModel` (MVI) + State/Event/Action +
  Koin module + 12-test suite. Flagged 3 idea-layer/contract gaps in the generation report: (a)
  `InvitationRepository.joinGroup`'s `rowId` param cannot be derived from `validateCode`'s result
  (no `id`/`rowId` field on the wire `InvitationRowDto`/domain `Invitation`) — passed as a
  documented `0L` placeholder that only affects the best-effort mark-accepted bookkeeping call,
  never the association call itself; (b) `ui.yaml#state_model.di` declares `GroupRepository` +
  `NetworkMonitor` but neither is required — `InvitationRepository.fetchGroupPreview`/`joinGroup`
  already absorb the calls `flow.yaml#dependencies.repositories.GroupRepository` describes, and
  no `NetworkMonitor`-based offline pre-check exists elsewhere in the codebase for this
  `business_logic.kind: processor` pattern (errors are read off the `NetworkError` the repository
  returns, same as `LoginSignupViewModel`); (c) `AuthSession.userId` is a `String`, not the
  numeric Fineract `clientId` `joinGroup` requires — resolved via `.toLongOrNull()` with a
  `crashReporter`-recorded recovery path (redirect to login-signup) on parse failure, since no
  dedicated `clientId` accessor exists on `AuthSession`/`AuthRepository`.
<!-- kmp-viewmodel-gen:END -->

<!-- kmp-screen-gen:BEGIN -->
## UI Surface (Screen / Route / Preview / TestTags)

**Container + Content** (`JoinWithCodeScreen.kt`) — `JoinWithCodeScreen` (Container, collects
`stateFlow`, consumes `JoinWithCodeEvent` via `EventsEffect`) + `JoinWithCodeContent`
(stateless, `state.deriveScreenState()`-driven `when`). 3 section composables:
`JoinCodeEntrySection` (Initial/Validating), `GroupPreviewSection` (Preview/Joining/Success),
`JoinCodeErrorSection` (the 4 `Error*` states). 4 reusable components under `components/`:
`JoinHeroIcon`, `InviteCodeField`, `GroupPreviewCard`, `JoinCodeErrorBanner`. See API.md#screen.

**Route** (`JoinWithCodeRoute.kt`) — `/groups/join`, co-located in this module (not
`cmp-navigation`), same convention as `LoginSignupRoute.kt`/`GroupTypePickerRoute.kt`. Pure
pass-through NavGraphBuilder registration — DC3 count-assertion 0/0/0. See API.md#route.

**Preview** (`JoinWithCodeScreenPreview.kt`) — 6 `@Preview` functions (top-level
`PreviewParameterProvider` with 8 `JoinWithCodeState` variants + 4 sub-composable previews),
data sourced from `demo-data.yaml#entries`. See API.md#preview.

**TestTags** (`JoinWithCodeTestTags.kt`) — 9 append-only constants. See API.md#tags.

**i18n** — 27 string keys under `screens_join_with_code_*` appended to
`src/commonMain/composeResources/values/strings.xml` (en only — other locales are a
translation-team follow-up per `training-layer/instructions/stream-first/latest/CORE_I18N.md`).

- 2026-07-22 — `kmp-screen-gen`: `JoinWithCodeScreen.kt` (Container+Content+3 sections) +
  4 components + `JoinWithCodeRoute.kt` + `JoinWithCodeTestTags.kt` (9 tags) +
  `JoinWithCodeScreenPreview.kt` (6 previews) + `strings.xml` (27 keys). Flagged follow-ups: (a)
  `GroupPreview.groupType`/`.roleToAssign` render via raw enum `.name` — no humanized
  `displayName` on the domain model (same gap as `GroupListCard.kt`); (b) the
  `app://groups/join?code={inviteCode}` deep-link URI is not yet registered as a platform
  `NavDeepLink`/intent-filter; (c) `joinWithCodeScreen(...)` is not yet wired into the app-wide
  `NavGraphBuilder` host (`RootNavNavigation`/`AuthenticatedNavbarNavigation`) — same pending gap
  as `login-signup`; (d) `KptTopAppBar`'s internal back-arrow `IconButton` has no per-button
  testTag slot, so `JoinWithCodeTestTags.BACK_BUTTON` tags the whole (single-affordance) top app
  bar container instead.
<!-- kmp-screen-gen:END -->
