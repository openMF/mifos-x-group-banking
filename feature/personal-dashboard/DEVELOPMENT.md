<!-- generated-by: kmp-viewmodel-gen -->
# feature/personal-dashboard — Development

## 1. Module Identity

`:feature:personal-dashboard` — the member home screen (`/dashboard/member`), unified-identity
(no `clientId`/`selfServiceToken` nav-param — resolved from the auth token). Namespace
`org.mifos.groupbanking.feature.personaldashboard`. Source of truth:
`idea-layer/screens/personal-dashboard/{ui,flow,data-flow,docs}.yaml`.

<!-- kmp-viewmodel-gen:BEGIN -->
## 2. Public API

See `PersonalDashboardViewModel.kt` class/field KDoc for the full ViewModel contract
(`PersonalDashboardState`, `PersonalDashboardEvent`, `PersonalDashboardAction`,
`PersonalDashboardScreenState`, `DashboardError`) — not re-documented here to avoid drift; see
`API.md#viewmodel` / `#state` / `#actions` / `#events` / `#di`.

## 3. Consumers

- `feature/personal-dashboard/src/commonTest/.../PersonalDashboardViewModelTest.kt` (this pass,
  13 tests).
- `cmp-navigation` — `PersonalDashboardModule` included in `KoinModules.kt#featureModule`, and
  `:feature:personal-dashboard` added as a `cmp-navigation` dependency. No `Route.kt`/`Screen.kt`
  exists yet for this feature — nav-graph wiring + the Compose screen layer are `kmp-screen-gen`
  scope, not generated by this pass.

## 4. Boundaries

Reads exclusively via the injected `MemberDashboardRepository.memberDashboardStream(selectedGroupId,
scope)` (`core/data`) — no direct API/DB access from this module. `business_logic.kind: crud`
(read-only, dynamic-key by group) per RULE-IMPLEMENT-STORE5-001 — the offline-first
`ScreenDataStream<MemberDashboard>` is consumed directly. `SessionManager.endSession()`
(`core-base/security`) is called for real on `ScreenState.Unauthenticated` — `flow.yaml#on_error`'s
`action: clear_session` branch — nothing else in `core-base` is written or bypassed.

## 5. Data

See `API.md#state` / `#actions` / `#events` for the ViewModel field/payload schema.

## 6. Errors

`DashboardError` (3 variants): `Network`, `Server` (both retryable), `Unauthorized` (non-retryable
— `error_session_expired`, and uniquely among this codebase's screens, wired to a REAL
`sessionManager.endSession()` call rather than just a display-only error state, since ui.yaml
declares `SessionManager` in `state_model.di` specifically for this branch).

## 7. Testing

`feature/personal-dashboard/src/commonTest/kotlin/org/mifos/groupbanking/feature/personaldashboard/PersonalDashboardViewModelTest.kt`
— 13 tests: initial state, first-mount default-group request, pool-model-adaptive Content mapping
(ACCUMULATING + ROTATING_PAYOUT), derived Empty screenState, group-switch re-fetch, OnRefresh
(bypass-and-refresh), OnRetry (SWR retry), OnSavingsCardClick (happy path + defensive no-op),
NoNetwork/Server/Unauthorized error mapping (the latter also asserting `sessionManager
.endSession()` fired for real). No `commonTest` UI-test suite yet for the screen layer — pending
`kmp-screen-gen` + `kmp-compose-uitest-gen`.

## 8. Observability

See `PersonalDashboardViewModel.kt` KDoc (Kermit `Logger.i/w` + `CrashReporter` breadcrumbs +
`KptAnalyticsTracker.trackSavingsOperation(...)`/`trackGroupOperation(...)` on mount and every
user-initiated read/group-switch/card-tap — real shipped methods, none invented).

## 9. Evolution

- 2026-07-22 — `kmp-viewmodel-gen`: `PersonalDashboardViewModel.kt` (state/action/event sealed
  types + pool-model-adaptive Content mapping + dynamic-key group-switch re-fetch + real
  `SessionManager.endSession()` on 401), `PersonalDashboardModule.kt` (Koin), feature module
  scaffold (`build.gradle.kts`, `settings.gradle.kts` include, `cmp-navigation` dependency +
  `KoinModules.kt` include), 13-test `PersonalDashboardViewModelTest.kt`. Flagged idea-layer gaps
  (not invented): (1) `PersonalDashboardEvent.NavigateToGroupList` is declared in
  `ui.yaml#state_model.events.members` with no corresponding `on_click` in
  `ui.yaml#components` — currently unreachable from any wired action; (2)
  `PersonalDashboardEvent.NavigateToSavings` carries `poolModel` (the closest available
  discriminator) rather than a full `typeConfig` object, since `MemberDashboard`/`GroupSummary`
  carry no `typeConfig` field.
<!-- kmp-viewmodel-gen:END -->

<!-- kmp-screen-gen:BEGIN -->
## 2b. Public API — screen layer

`PersonalDashboardScreen.kt` (Container `PersonalDashboardScreen` + stateless
`PersonalDashboardContent` + `internal` sub-composables), `PersonalDashboardRoute.kt`
(`PersonalDashboardRoute` + `navigateToPersonalDashboard` + `personalDashboardScreen` NavGraphBuilder
extension), `PersonalDashboardTestTags.kt`, `PersonalDashboardScreenPreview.kt`, and
`components/{GroupSelectorChipRow,SavingsSummaryCard,ShareoutProjectionCard,RecentActivityRow}.kt`
now exist — see `API.md#screen` / `#route` / `#preview` / `#tags`.

## 3b. Consumers — screen layer

`PersonalDashboardRoute.kt`'s `personalDashboardScreen(onNavigateToSavings, onNavigateToGroupList)`
NavGraphBuilder extension is ready for the `cmp-navigation` host to register — not yet wired into
the app's root nav graph (out of this pass's scope; `group-list-screen`'s own `groupListScreen(...)`
extension is similarly not yet registered, so this mirrors the existing project state rather than
introducing a new gap).

## 7b. Testing — screen layer

No `commonTest` Compose UI-test suite yet for `PersonalDashboardScreen.kt` — pending
`kmp-compose-uitest-gen` / `kmp-journey-uitest-gen`.

## 9b. Evolution — screen layer

- 2026-07-22 — `kmp-screen-gen`: `PersonalDashboardScreen.kt` (Container+Content, state-driven
  Loading/Content/Empty/Error rendering, pool-model-adaptive `ShareoutProjectionCard`, multi-group
  `GroupSelectorChipRow`, KptScaffold pull-to-refresh), `PersonalDashboardRoute.kt`,
  `PersonalDashboardTestTags.kt` (12 tags + 2 resolver functions), `PersonalDashboardScreenPreview.kt`
  (13 `@Preview` functions, demo-data-sourced), 4 reusable `components/*.kt` files, 25 i18n string
  resources (`strings.xml`), `build.gradle.kts` gained `implementation(projects.core.common)` for
  `formatGrouped`. Flagged idea-layer gap (not invented): `preview/empty.html`'s "Browse Groups" /
  "Create a Group" CTA buttons have no matching `PersonalDashboardAction` member or
  `ui.yaml#states.empty.components` entry — Empty state renders illustration + title + body only
  per RULE-IMPL-DEAD-CLICKABLE-001 Rule 1.
<!-- kmp-screen-gen:END -->
